PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Database configuration
DB_DIALECT=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nexora_computers
DB_USER=postgres
DB_PASSWORD=omar1906

# Fallback DB path for SQLite mode (if DB_DIALECT=sqlite)
DB_STORAGE=./database.sqlite

# Security & JWT
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# File uploads
UPLOAD_PATH=uploads

# Payment Integrations
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
